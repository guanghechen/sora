{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "disposable.types",
  "sourceRoot": "packages/disposable.types/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/disposable.types",
        "parallel": false,
        "commands": ["rimraf lib"]
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["clean", "^build"],
      "options": {
        "cwd": "packages/disposable.types",
        "parallel": false,
        "sourcemap": true,
        "commands": ["cross-env ROLLUP_CONFIG_TYPE=lib rollup -c ../../rollup.config.mjs"]
      },
      "configurations": {
        "production": {
          "sourcemap": false,
          "env": {
            "NODE_ENV": "production"
          }
        }
      }
    },
    "watch": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/disposable.types",
        "parallel": false,
        "sourcemap": true,
        "commands": ["cross-env ROLLUP_CONFIG_TYPE=lib rollup -c ../../rollup.config.mjs -w"]
      }
    }
  }
}
